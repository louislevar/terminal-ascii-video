# terminal-ascii-video
Converts videos into animated ASCII playback directly in the terminal with configurable rendering, saved animation generation, and replay support using Python, OpenCV, and Pillow.
